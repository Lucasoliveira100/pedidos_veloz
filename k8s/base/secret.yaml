# Exemplo de referência apenas - NÃO commitar valores reais em base64.
# Em produção este manifest deveria ser gerado pelo pipeline a partir de
# um cofre externo (ex.: Vault, AWS Secrets Manager, Sealed Secrets),
# nunca versionado em texto claro no Git.
apiVersion: v1
kind: Secret
metadata:
  name: veloz-db-credentials
  namespace: pedidos-veloz
type: Opaque
stringData:
  DATABASE_URL: "postgresql://veloz:SUBSTITUIR_NO_PIPELINE@postgres:5432/pedidos_veloz"
